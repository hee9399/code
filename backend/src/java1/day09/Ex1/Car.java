package java1.day09.Ex1;

public class Car {
	// 1. 필드 : * 객체의 데이터 를 저장하는곳
	
	// 2. 생성자 : * 객체 생성시 초기화 역할 담당
		// * 생성자명 : 클래스명ㅇ과 동일 
		// * 메소드와 비슷하지만 리턴return 없다. 
		// * 생성자명( 타입 매개변수 , 타입 매개변수 , 타입 매개변수){실행코드}
		// 실행조건 : 1.매개변수로 전달된 값을 필드에 저장시킨다 (생성자의 역할)
				// 2.(안전상) 필드에 저장하가ㅣ 전에 가공/유호성검사
		// 필드는 실제 데이터 저장되는곳 --> 객체.필드 (권장하지않는다.)
	
	// 1. 기본생성자
	Car(){}
	
	// 2. 매게변수 3개인 생성자 정의
	Car(String model , String color , int maxSpeeed){
		
	}
	// 3. 메소드
}
